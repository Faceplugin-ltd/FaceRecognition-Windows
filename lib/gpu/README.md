# GPU runtime (pending)

Paste ONNX Runtime CUDA providers and CUDA/cuDNN DLLs into `FaceRecognition-Windows-SDK/source_libraries/`, then re-run `python scripts/prepare.py`.

Until then, use `lib/cpu/` with `FACEPLUGIN_BACKEND=cpu`.
